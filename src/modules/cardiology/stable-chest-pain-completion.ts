import type { Scenario } from '@anesthesia/scenarios/types';
import type { CompletionRequirementAudit } from '@platform/catalog/scenario-completion';
import { STABLE_CHEST_PAIN_EVALUATION } from './scenarios/stable-chest-pain-evaluation';
import { STABLE_CHEST_PAIN_FIXTURES } from './stable-chest-pain-fixtures';
import { STABLE_CHEST_PAIN_TUTOR_VERSION } from './tutor/stable-chest-pain-guidance';
import { STABLE_CHEST_PAIN_DEMONSTRATION_VERSION } from './demo/stable-chest-pain-demonstration';

/**
 * Exact-version evidence for one lesson, and no claim about any other.
 *
 * Five objectives rather than six, so unlike every pediatrics lesson this one
 * satisfies the shared observable-objectives cap and leaves only the two
 * runtime requirements outstanding.
 */
export function stableChestPainCompletionEvidence(scenario: Scenario, capabilityVersion: string, moduleId: string): readonly CompletionRequirementAudit[] {
  if (moduleId !== 'cardiology' || capabilityVersion !== '0.1.0-alpha.48'
    || scenario.metadata.id !== STABLE_CHEST_PAIN_FIXTURES.scenarioId
    || scenario.metadata.version !== '0.1.1' || STABLE_CHEST_PAIN_FIXTURES.contentVersion !== '0.1.1'
    || STABLE_CHEST_PAIN_FIXTURES.seed !== 1624
    || STABLE_CHEST_PAIN_TUTOR_VERSION !== '0.1.0'
    || STABLE_CHEST_PAIN_DEMONSTRATION_VERSION !== '0.1.0'
    || JSON.stringify(scenario) !== JSON.stringify(STABLE_CHEST_PAIN_EVALUATION)) return [];
  return [
    { id: 'deterministic-seed-policy', status: 'satisfied', evidence: ['stable-chest-pain-fixtures.ts binds seed 1624 and content 0.1.1 to expert, investigate-before-estimate error, recovery, and no-action paths. The three-month exertional history with its onset at about six minutes, resolution within four minutes of rest, and two-to-three-times-weekly frequency without progression, the absent rest, prolonged, syncopal, dyspneic and diaphoretic features, the resting observations, the fixed resting ECG report of sinus rhythm without ischemic ST-T change, the risk factors including hypertension, current tobacco use and an LDL of 168 mg/dL, and the authored not-very-low likelihood band are authored constants; no coronary, ischemic or event model is claimed, and no outcome follows from any choice. tests/integration/stable-chest-pain-runs.test.ts replays every path frame-for-frame across all three guidance levels and both practice regions.'] },
    { id: 'meaningful-progression', status: 'satisfied', evidence: ['The lesson advances through five recorded steps on the shared simulation clock in a strict order, and it is the first lesson given evidence in this repository with no time gate anywhere — appropriately, because nobody is deteriorating and nothing is racing. What the engine enforces is the order of reasoning itself: the trajectory before the characterization, the characterization before the likelihood, the likelihood before any testing intent, and the testing intent before the follow-up and safety net. Each step refuses until its predecessor is recorded, and repeating a recorded step changes nothing.'] },
    { id: 'meaningful-actions-and-choices', status: 'satisfied', evidence: ['Five declared decisions verify a stable trajectory while preserving explicit acute-change triggers; characterize the symptom by what it actually does rather than by a label; integrate age, sex, symptoms, hypertension, tobacco use, LDL, examination findings and the resting ECG report into a likelihood band; record a patient-specific noninvasive-testing intent through shared decision-making against a local pathway; and record follow-up with the acute-change safety net. The lesson examines nobody, acquires or interprets no ECG, calculates no risk score, measures no exercise capacity, orders or performs no test, diagnoses no coronary disease or ischemia, prescribes no medication, determines no disposition, and predicts no event or outcome. No exact score and no universal modality is supplied, and the selection factors named — the clinical question, test strengths and limitations, exercise capacity, ECG interpretability, radiation and contrast, comorbidity, preference, access, local expertise and local quality — are the choice rather than a preamble to a fixed answer.'] },
    { id: 'bounded-stop-condition', status: 'satisfied', evidence: ['The branch ends at the recorded follow-up and safety net with no diagnosis, no test ordered and no disposition. Later actions cannot restart an ended branch, and the ending certifies clinicalLikelihood as the authored not-very-low band rather than a number, exactScoreCalculated false, and testPerformed false. The acute-change triggers that would bring the patient back sooner are part of the ending rather than an appendix to it.'] },
    { id: 'debrief-and-counterfactual', status: 'satisfied', evidence: ['src/modules/anesthesia/ui/Debrief.tsx maps all five objectives to accepted engine events; the no-action path meets none and the expert path meets all five. The counterfactual is the reflex the lesson exists to interrupt: the common-error path verifies the trajectory and characterizes the symptom correctly, then reaches for a testing pathway without ever estimating the likelihood that decides whether testing is appropriate at all, meeting two of five. The recovery path additionally characterizes before establishing the trajectory, takes the same investigate-before-estimate refusal, and still completes in order from the same positions.'] },
    { id: 'reference-transcripts', status: 'satisfied', evidence: ['stable-chest-pain-fixtures.ts binds exact-content expert, investigate-before-estimate error, recovery, and no-action pathways to seed 1624 for deterministic replay through the shared engine, including both order refusals.'] },
    { id: 'guidance-and-demonstration', status: 'satisfied', evidence: [`Five observed-state prompts at version ${STABLE_CHEST_PAIN_TUTOR_VERSION} read the learner's own recorded steps. This is the first cardiology lesson to carry a tutor, and its register is deliberately unlike the emergency lessons that came before it: nobody is deteriorating, and the failure modes are a word, a number and a reflex. The word is "atypical", which the pattern beat refuses by name — it has meant everything from "not classic angina" to "probably nothing", it performs worse in women, and contemporary guidance has dropped it, so the beat records what happens, when, for how long and what relieves it instead. The number is an exact risk score: the likelihood beat says the authored answer is a band rather than a percentage, because the decision that follows turns on the band and not on a second decimal place, and it names the resting ECG as routinely over-read since a normal tracing in an asymptomatic patient tells you very little. The reflex is testing before estimating, which the engine ordering refuses and the testing beat answers by asking what question a test would answer first — with no universal modality, and with local access, expertise and quality named as part of the choice, since the best test on paper performed badly nearby is not the best test for this patient. The safety-net beat treats the close of a calm visit as the part that keeps working after the patient leaves. Worked example ${STABLE_CHEST_PAIN_DEMONSTRATION_VERSION} drives the ordinary controls through the real engine to the recorded safety net, and its narration is generated from the tutor's own prose so the two cannot drift apart. A test asserts that no beat anywhere calls the symptom atypical, states a percentage likelihood, names a specific test as the answer, or diagnoses coronary disease. tests/unit/stable-chest-pain-demonstration.test.ts verifies observed response and every beat against the real engine, and tests/ui/cardiology-stable-chest-pain.test.tsx verifies the tray, silence when unassisted, stable controls while watching, and version binding.`] },
    { id: 'inclusive-runtime-verification', status: 'missing', evidence: ['Automated UI, observed-state and replay checks exist. Exact-version assistive-technology, keyboard, phone, zoom, reduced-motion, offline, and performance validation remains pending.'] },
    { id: 'report-control-coverage', status: 'missing', evidence: ['The scenario inherits the shared report control and has an exact-version Worker catalog record. Complete four-surface runtime evidence is not yet bound.'] },
  ];
}
