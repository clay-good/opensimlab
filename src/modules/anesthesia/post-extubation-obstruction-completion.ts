import type { Scenario } from './scenarios/types';
import type { CompletionRequirementAudit } from '@platform/catalog/scenario-completion';
import { POST_EXTUBATION_OBSTRUCTION } from './scenarios/post-extubation-obstruction';
import { POST_EXTUBATION_OBSTRUCTION_FIXTURES } from './post-extubation-obstruction-fixtures';
import { TUTOR_RULES } from './tutor/guidance';
import { POST_EXTUBATION_OBSTRUCTION_DEMONSTRATION_VERSION } from './demo/post-extubation-obstruction-demonstration';

/** The tutor version this evidence is bound at: every rule carries the same one. */
const TUTOR_VERSION = TUTOR_RULES[0]?.version ?? 'unbound';

/** Exact-version evidence for one lesson, and no claim about any other. */
export function postExtubationObstructionCompletionEvidence(scenario: Scenario, capabilityVersion: string, moduleId: string): readonly CompletionRequirementAudit[] {
  if (moduleId !== 'anesthesia' || capabilityVersion !== '0.1.0-alpha.48'
    || scenario.metadata.id !== POST_EXTUBATION_OBSTRUCTION_FIXTURES.scenarioId
    || scenario.metadata.version !== '0.1.0'
    || POST_EXTUBATION_OBSTRUCTION_FIXTURES.contentVersion !== '0.1.0'
    || POST_EXTUBATION_OBSTRUCTION_FIXTURES.seed !== 4820
    || TUTOR_VERSION !== '0.1.0'
    || TUTOR_RULES.some((rule) => rule.version !== TUTOR_VERSION)
    || POST_EXTUBATION_OBSTRUCTION_DEMONSTRATION_VERSION !== '0.1.0'
    || JSON.stringify(scenario) !== JSON.stringify(POST_EXTUBATION_OBSTRUCTION)) return [];
  return [
    { id: 'deterministic-seed-policy', status: 'satisfied', evidence: ['post-extubation-obstruction-fixtures.ts binds seed 4820 and content 0.1.0 to expert, oxygen-alone error, late-bundle recovery, and no-action paths over 3,000 ticks. The numbers the counterfactual turns on are solver output on the bounded obstruction trajectory the scenario declares: a modelled patency held at 0.50, a tidal volume halved from 500 to 250 mL, an end-tidal carbon dioxide climbing to 55 mmHg, and a saturation that reaches 100% on the path where none of that is treated. tests/integration/anesthesia-post-extubation-obstruction-runs.test.ts replays every path frame-for-frame across all three guidance levels and both practice regions.'] },
    { id: 'meaningful-actions-and-choices', status: 'satisfied', evidence: ['An empty formulary and three controls: the airway help request, the ventilator, and the bounded jaw-thrust-with-CPAP maneuver. The maneuver is held for a fixed number of seconds rather than latched on, which is the honest abstraction of a physical manoeuvre someone has to keep doing, and the engine records the intent rather than the technique. The second objective asks for the maneuver AND active breath delivery at 95% oxygen or more together, and the transcripts exist mainly to show that the conjunction is doing real work rather than describing one action twice.'] },
    { id: 'bounded-stop-condition', status: 'satisfied', evidence: ['The scripted pattern begins at tick 100 and the transcripts end at 3,000, past the scenario\'s own reassessment prompt at 1,000 and past the point where every path has settled: the treated paths hold a patency of 1.00 with a tidal volume of 500 mL, and the untreated ones hold 0.50 and 250 mL with a carbon dioxide still climbing at the end of the run. The third objective reads the recovery back, so the trace must run long enough for its absence to be unambiguous.'] },
    { id: 'debrief-and-counterfactual', status: 'satisfied', evidence: ['src/modules/anesthesia/ui/Debrief.tsx derives all three objectives from the replayed state trace and the recorded actions rather than from a score kept during the run. The counterfactual is an intervention that improves the monitored number and treats nothing, and here it does so more completely than in any other lesson in the module. The error path establishes active breath delivery at 100% oxygen and does nothing else. Measured at tick 2,900: saturation 100%, modelled patency 0.50, tidal volume 250 mL, end-tidal carbon dioxide 55 mmHg. The no-action path at the same tick reads a saturation of 97% with identical patency, volume and carbon dioxide -- so the treated-looking run has the BETTER oximeter reading and precisely the same airway. It scores not met on all three. The expert path meets all three; the recovery path adds the help request and the maneuver late and reads partly met, partly met, met.'] },
    { id: 'reference-transcripts', status: 'satisfied', evidence: ['post-extubation-obstruction-fixtures.ts binds exact-content expert, oxygen-alone error, late-bundle recovery, and no-action pathways to seed 4820 for deterministic replay through the shared engine. Two readings are pinned by tests. The first is that NEITHER half of the bundle does anything alone, which is not inferable from the objective text and is easy to assume otherwise: a held jaw thrust with CPAP and no breath delivery leaves the patency at 0.50, the tidal volume at 250 mL and the carbon dioxide at 55 -- identical to the untreated run to the digit -- and breath delivery without the maneuver does exactly the same. Only the conjunction moves anything, and it moves everything: patency 1.00, tidal volume 500 mL. The second is the saturation, which is the wrong instrument throughout this lesson. An untreated obstructing patient holds 97%, and the oxygen-alone path holds 100% while nothing about the obstruction has changed. A learner watching the oximeter would rank these two runs in the wrong order.'] },
    { id: 'guidance-and-demonstration', status: 'satisfied', evidence: [`The nine authored tutor rules at version ${TUTOR_VERSION} in tutor/guidance.ts apply here and are silent on the unassisted setting and suppressed while any alarm is active. None names the conjunction the second objective requires, which is correct: that the maneuver and the delivery are halves of one action rather than alternatives is the judgement being taught. Worked example ${POST_EXTUBATION_OBSTRUCTION_DEMONSTRATION_VERSION} is the module's twenty-eighth observed-state one and the fifth to open with a beat that deliberately does nothing, since the pattern arrives at tick 100 and both timed objectives measure from it. One gate is worth naming because it was found by failing: the example ends when the airway is open, and the airway is ALSO open before the scripted obstruction arrives, so a first draft gated purely on patency finished at tick one having done nothing. The closing gate now additionally requires the latched help tick and active delivery, which cannot be true before the pattern. Its other gates are chosen around the maneuver being HELD rather than latched -- the beat after it reads the patency, a quantity that only rises once the bundle is complete, so no beat can walk backwards while the hold decays. Its delivery beat says plainly that on its own it will not open anything, before the learner watches that be true. Its closing beat declines the credit the demonstration would take, reporting that the oxygen-alone run reads a better saturation than the untreated one and naming the tidal volume and capnogram as the instruments to watch instead. tests/unit/anesthesia-post-extubation-obstruction-worked-example.test.ts drives every beat through the real engine from both a fresh start and a handover, and tests/unit/worked-example-offer.test.ts pins that what this audit claims is what the product actually offers.`] },
    { id: 'inclusive-runtime-verification', status: 'missing', evidence: ['Automated observed-state and replay checks exist. Exact-version assistive-technology, keyboard, phone, zoom, reduced-motion, offline, and performance validation remains pending.'] },
    { id: 'report-control-coverage', status: 'missing', evidence: ['The scenario inherits the shared report control and has an exact-version Worker catalog record. Complete four-surface runtime evidence is not yet bound.'] },
  ];
}
